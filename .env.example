# Copie para .env.local e preencha com o projeto Supabase da BICOJÁ (nunca a service_role key aqui).
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Chave pública VAPID para notificações push (não é segredo, só a privada é).
# Gere o par com: npx web-push generate-vapid-keys
VITE_VAPID_PUBLIC_KEY=
